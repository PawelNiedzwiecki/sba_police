// import React, { useState, useEffect } from 'react';

// const ransformationInput = (props) => {

//     const
//         [projectType, setProjectType] = useState(props.projectType);

//     return (
//         <input type="text" className="form-control transformation" placeholder="Link SBA" aria-label="SBALink" aria-describedby="addon-wrapping" value={sbaLink} onChange={(e) => setSbaLink(e.target.value)} disabled={(projectType == 'review') ? "disabled", "no"}/>
//     )

// }

// export default transformationInput